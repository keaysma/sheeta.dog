{
  "name": "sheeta.dog",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "d": "bun run src/index.ts --hot",
    "build": "bun build src/index.ts --target=bun --outdir out --outfile server.ts --minify",
    "serve": "bun run out/index.js"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}